server {
    listen 80;
    server_name localhost;
    root /usr/share/nginx/html;
    index index.html;

    # Все запросы к статическим файлам
    location / {
        try_files $uri $uri/ =404;
    }
}
